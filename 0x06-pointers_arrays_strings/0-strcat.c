#include "main.h"

/*
 * _strcat - function that concanat two str
 * @dest : pointer to destation input 
 *@ src : pointer to sorce input
 *Returns : pointer to string @dest
 */

char *_strcat(char *dest, char *src);
}
int c , c2;

c = 0;
while )dest[c])
c++;
for (c2 = 0; src [c2]; c2++)
dest[c++] = src[c2];
retrun (dest);
}
